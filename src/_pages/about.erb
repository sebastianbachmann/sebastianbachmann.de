---
title: About
description: |
  Sebastian Bachmann, Software- und Produktentwickler in Elternzeit..
permalink: /about/

---

<%= render UI::Container.new(class_name: "mt-16 sm:mt-32") do %>
  <div class="grid grid-cols-1 gap-y-16 lg:grid-cols-2 lg:grid-rows-[auto_1fr] lg:gap-y-12">
    <div class="lg:pl-20">
      <div class="max-w-[250px] px-2.5 lg:max-w-none">
        <figure>
          <img src="/images/sebastian-bachmann.png" alt="Sebastian Bachmann" class="aspect-square sm:rotate-3 rounded-2xl bg-zinc-100 object-cover">
          <figcaption class="sr-only">Sebastian Bachmann</figcaption>
        </figure>
      </div>
    </div>

    <div class="lg:order-first lg:row-span-2">
      <h1 class="text-4xl font-bold tracking-tight text-zinc-800 sm:text-5xl">
        Sebastian Bachmann, <span class="block">Software- und Produktentwickler</span> in Elternzeit.
      </h1>

      <div class="prose">
        <%= render_snippet "about" %>
      </div>
    </div>

    <div class="lg:pl-20">
      <ul role="list">
        <% socials = site.data.socials %>
        <%= render WideSocialLink.new(href: socials.email.href, label: socials.email.handle, icon: socials.email.icon) %>
        <%= render WideSocialLink.new(href: socials.twitter.href, label: socials.twitter.title, icon: socials.twitter.icon, class_name: "mt-8 border-t border-zinc-100 pt-8") %>
        <%= render WideSocialLink.new(href: socials.github.href, label: socials.github.title, icon: socials.github.icon, class_name: "mt-4") %>
        <%= render WideSocialLink.new(href: socials.mastodon.href, label: socials.mastodon.title, icon: socials.mastodon.icon, class_name: "mt-4") %>
        <%= render WideSocialLink.new(href: socials.linkedin.href, label: socials.linkedin.title, icon: socials.linkedin.icon, class_name: "mt-4") %>
      </ul>
    </div>
  </div>
<% end %>
